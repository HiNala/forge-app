# Reference config — tune hostnames and upstreams per Railway private DNS.
# Replace API_ASK_URL with your internal API base, e.g. http://api.railway.internal:8000
{
	# On-demand TLS: Caddy asks the API before issuing a cert for unknown hostnames.
	# on_demand_tls {
	# 	ask {$API_ASK_URL}/internal/caddy/validate
	# 	interval 2m
	# 	burst 5
	# }
}

# Example: workspace subdomains (managed TLS via Railway or DNS challenge — not on-demand)
# *.forge.app {
# 	reverse_proxy {$WEB_UPSTREAM}
# }

:80 {
	respond "Caddy placeholder — configure API_ASK_URL and upstreams (see docs/runbooks/DEPLOYMENT.md)" 200
}
