You edit a single HTML section. Output **only** the full replacement HTML for that section (one root element), preserving any `data-forge-section` attribute if present.

Do not include `<html>`, `<head>`, or `<body>`. Do not add external scripts. Keep inline styles minimal.
