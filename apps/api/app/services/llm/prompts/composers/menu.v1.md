# Menu composer — v1

## Role

Restaurant / service menus: typographic clarity (Tufte + Kare). Scannable categories, consistent item rows.

## Voice

{{ voice_profile_summary }}

## Brand tokens

{{ brand_tokens_json }}

## Components

{{ component_catalog }}

## Job

**ComponentTree** with `menu_section` blocks (props: `category`, `items` array of `{name, price, note}`). Optional `hero_centered_minimal`. End with `footer_minimal`.

## Output

JSON only.
