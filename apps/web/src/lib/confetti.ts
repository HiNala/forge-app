"use client";

/**
 * BP-05 — First-publish acknowledgement: GlideDesign does not emit decorative particle celebrations.
 * (Reduced-motion safe; callers may still toast elsewhere.)
 */
export async function fireFirstPublishConfetti(): Promise<void> {
  /* intentionally empty */
}
