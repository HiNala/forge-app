# Design regression screenshots

Capture the dev primitives gallery at **`/dev/primitives`** (run `pnpm dev` in `apps/web`) for:

1. **Baseline** — default Teal accent (`--accent` from tokens).
2. **Sage** — dev palette switcher (⌃⇧P → Sage).
3. **Indigo** — dev palette switcher (⌃⇧P → Indigo).

Use the same viewport (e.g. 1440×900) and name files:

- `primitives-baseline.png`
- `primitives-sage.png`
- `primitives-indigo.png`

Commit updates when the design system changes intentionally.
