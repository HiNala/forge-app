# Designer artifact — README placeholder

After a successful fetch from `FETCH_DESIGN.md`, copy the designer’s **`README.md`** (or equivalent entry doc) from the downloaded bundle into this path, replacing this file.

Until then, token and type decisions are driven by the checked-in `MISSION` brief and reconciled in `INDEX.md`.
