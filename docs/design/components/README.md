# Design component references

The mission asks for Figma-style exports (screenshots/SVG) for button, input, tabs, card states, etc., from the **upstream** `artifact.json` / design tool when available.

**Status:** No component raster exports are checked in until the Anthropic design artifact is fetched with API access. Use:

- `apps/web/src/app/dev/design-system` (development only) — live matrix of every primitive state.
- Historical HTML prototypes (when present locally): `Forge_Landing_Page_v2.html`, `Forge_App_v6.html` — interaction patterns only; not a token source.

Replace this folder with dated screenshots when the design pack is imported.
